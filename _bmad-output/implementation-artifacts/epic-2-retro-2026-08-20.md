# Rétrospective Epic 2 — 2026-08-20

**Type :** clôture d’**implémentation**. Remplace la rétro partielle de cadrage du même jour.  
**Verdict machine :** **acceptée** — 6/6 stories `done`.  
**Opportunité :** [opp-2](../planning-artifacts/opportunities/opp-2-upgrade-v1-x-substrat-migration-workspace.md)  
**Epic :** [epic-2](../planning-artifacts/epic-2-upgrade-v1-x-substrat-migration-workspace.md)  
**Plan 2.1 :** [upgrade-v1-x-file-plan.md](../planning-artifacts/upgrade-v1-x-file-plan.md)  
**Baseline :** AEF 1.1.2 (non bumpée) / `364ed16d9dbeb3968b2d14448589cd998a71eb12`

## Résumé d’exécution

opp-2 → SPEC → Epic 2 → validate → **dev 1d–1g YOLO**. Cinq formes CLI `aef upgrade`. Journal `aef.upgrade-transaction/v1` distinct d’EVALUATE. Cible productive `1.0.0`. Suite **1084 passed**, 10 skipped. Delivery Loop non lancé. Pas de tag ni Release.

## Stories et statuts

| Clé | Statut | Livrable principal |
|---|---|---|
| `epic-2-1-audit-brownfield-plan-fichiers` | done | Plan de fichiers ; `upgrade_compat.py` nommé ; lab non exposé |
| `epic-2-2-contrats-upgrade-v1` | done | Planificateur, bornes, schéma journal, ledger étendu |
| `epic-2-3-cli-check-dry-run` | done | `--check` / `--dry-run`, enveloppe `UPGRADE`, zéro écriture |
| `epic-2-4-appliquer-transaction` | done | Apply + journal prepared/committed + ledger + replay |
| `epic-2-5-recuperer-auditer` | done | Recover, AUDIT `upgrade-recovery-required`, exclusion RECORD |
| `epic-2-6-cloisonnement-documentation` | done | Isolation sentinelle + `docs/commands.md` / `docs/recovery.md` |

## Forces

- Update ≠ Upgrade tenu jusqu’au code : pas de `--target-schema`, pas d’`aef update`.
- `upgrade_project` / `apply_framework_release` restent du lab ; CLI nouvelle.
- Journal UPGRADE distinct ; `transaction_id` calculé hors bytes ledger (évite un cycle).
- Workspace `1.0.0` réel → `NO_CHANGE` ; registre `1.0.0→1.1.0→1.2.0` = tests seulement.
- Writer EVALUATE / format `evaluation-transaction.json` inchangés.
- Isolation : sentinelles extérieures absentes des sorties ; `AGENTS.md` / `CLAUDE.md` préservés, jamais créés.

## Difficultés / risques restants

- `cli.py` et `filesystem.py` restent des fichiers chauds (garde EVALUATE + UPGRADE).
- Recover `committed` + hash after exact : couvert par le moteur ; le test CLI public insiste surtout sur `prepared` + journal malformé.
- Finding AUDIT public = `upgrade-recovery-required` (tirets, comme EVALUATE), pas l’underscore de la prose SPEC.
- Validation utilisateur réelle (U5) = porte manuelle, hors tests.

## AC Epic — preuve

1. `1.0.0` → check / dry-run / apply / replay `NO_CHANGE` — `test_check_and_dry_run_no_change_on_valid_v1`.
2. Registre synthétique → apply `CHANGE`, replay `NO_CHANGE` — `test_apply_replay_and_ledger`.
3. Check / dry-run sans journal — snapshot mtime/bytes.
4. Future / trou / cycle / ambiguïté / ledger / bornes → `BLOCKED` — `test_upgrade_plan`.
5. Journal présent → check `BLOCKED` ; recover `prepared` puis replay sûr — `test_cli_upgrade`.
6. AUDIT : `upgrade-recovery-required` ≠ `evaluation-recovery-required`.
7. `instance_id` / `created_at` préservés après apply.
8. Fixture extérieure + env sentinelles — `test_upgrade_isolation`.
9. Suite verte ; version 1.1.2 inchangée.

## Dette ou sujets suivants

1. Porte humaine U5 avant toute Release.
2. CHANGELOG / tag / Release : workflow publication séparé, pas ce lot.
3. Aucun epic 3. Ne pas ouvrir Update réseau, V2 ou UI sur ce substrat sans nouvel opp GO.

## Préparation epic N+1

Aucun. Hors périmètre volontaire inchangé (Update, V2, portefeuille, UI, télémétrie).

## Assumptions

- Clôture documentaire : `docs/` déjà mis à jour en story 2.6 ; cette commande ne les réécrit pas.
- Bloc Git **SKIP** : pas de demande de commit, pas de branche `staging` AEF.
